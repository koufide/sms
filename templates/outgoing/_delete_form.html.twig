<form method="post" action="{{ path('outgoing_delete', {'id': outgoing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ outgoing.id) }}">
    <button class="btn">Delete</button>
</form>
